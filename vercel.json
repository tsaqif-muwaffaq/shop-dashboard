{
  "buildCommand": "npm rebuild esbuild && npm rebuild rollup && npm run build",
  "installCommand": "npm ci --ignore-scripts",
  "outputDirectory": "dist",
  "framework": "vite",
  "nodeVersion": "20.x",
  "env": {
    "CI": "false"
  }
}
